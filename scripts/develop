#!/usr/bin/env bash
set -euo pipefail

# Crée le dossier config et monte l'intégration
mkdir -p config/custom_components
ln -sfn "$(pwd)/custom_components/pronote" config/custom_components/pronote

# Lance Home Assistant en mode debug avec l'intégration montée
python -m homeassistant --debug -c config
